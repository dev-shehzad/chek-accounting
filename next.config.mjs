/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['localhost'], // Allow serving uploaded images
    },
  };
  
  export default nextConfig;
  